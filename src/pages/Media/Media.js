import './Media.css'

const Media = ({title, }) => {


    return (
        <div>
            <h1>Media Title</h1>
            <h4>Additional Info ie the other attributs in the row</h4>
            <p>Only if this is a piece of physical media will this button exist</p>
            <button> Reserve</button>
        </div>
    )
}

export default Media;